name: test app

on:
    push:
        branches:
            - main
            - "release/*"
            - "hotfix/*"
        paths:
            - "src/**"
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v2

            - name: run tests
              run: npm test